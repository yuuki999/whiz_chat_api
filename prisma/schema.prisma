datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// モデルの定義を追加
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}

// TODO: 他のモデルも必要に応じて追加
